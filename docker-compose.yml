services:
  frontend-app:
    build:
      context: .
      target: builder
    working_dir: /app

  app:
    build:
      context: .
      target: runtime
    ports:
      - "127.0.0.1:10001:80"
    restart: unless-stopped
